uses time.Calendar
uses time.DateUtil
uses libs.network.http.HTTPUtil

interface LatencyStore {
    int getMilisecondsExecutionTimeSince(DateTime start)
    dec getCurrentAverageLatency()
    void storeExecutionTime(int miliseconds, opt HTTPMessage req)
}
